import React from "react";
import Banner from "../components/Banner";

type Props = {};

const Shop = (props: Props) => {
  return (
    <div>
      <Banner />
    </div>
  );
};

export default Shop;
